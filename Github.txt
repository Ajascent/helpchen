cloning a repo
navigate to the main page of the repo
from the list of files, click 'code'
copy the URL for the repo, HTTPs,which will contain your Username and email
open terminal then change the current working directory to the location where you want the cloned directory 
type git clone and paste the URL (username & repo)
press enter to create the clone.
